# ??? ?? ???? ???? 5?? ~6???? ????? ?? ?? ? ??? ????? ???? ??
server:
  port: 0

spring:
  application:
    name: 1st-service

eureka:
  # ?? ??? ???? Eureka Server ui?? ??? ?? ???? ??? instance id ??
  instance:
    instance-id: ${spring.application.name}:${spring.instance.instance-id:${random.value}}
  client:
    # Eureka Server?? Service Registry ? ????? ??
    fetch-registry: true
    # Eureka Server? ?? ????? ????? ??
    register-with-eureka: true
    service-url:
      # Eureka Server? ?? URL ??
      defaultZone: http://localhost:8761/eureka