var undef/* = undefined (명시적 작성은 권장x) */; // 자바스크립트 엔진이 변수 초기화 시 undefined 사용
console.log(undef);

// null은 변수에 값이 없다는 것을 의도적으로 명시할 때 사용한다.
var nullVar = 'something';
nullVar = null;
console.log(nullVar);
